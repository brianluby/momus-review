/target
/reviews

# Local secrets and report artifacts stay out of the repo (docs/security.md).
.env
.env.*
!.env.example

# Per-developer tooling: rusty-brain MCP wiring + omp extension.
.mcp.json
.omp/